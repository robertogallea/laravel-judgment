# Issue tracker

Issues live in GitHub Issues for `robertogallea/laravel-judgment` (private). Use the `gh` CLI with `--repo robertogallea/laravel-judgment`, since this working copy has no git remote yet.

Triage labels: `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix` (only `ready-for-agent` exists so far; create others on first use).
